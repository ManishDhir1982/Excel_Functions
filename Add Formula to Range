Function fn_Add_Formula_To_Range(in_rng_for_Formula As Range, in_str_Formula As String)
' This function Adds a Formula provided in String to the Range Object

'What it needed to get the function running
  'It needs a Range Object to Insert the Formula to - Example: ActiveSheet.Range("B3:B10")
  'It need the Formula in String (text) format - example: =SUM(A1:A2)

' 1 - Put the Formula in the first Cell of the Provided Range
in_rng_for_Formula.Cells(1, 1).Formula = in_str_Formula

' 2 - Copy the Cell that has the formula
in_rng_for_Formula.Cells(1, 1).Copy

' 3 - Paste the First Cell Value to the Entire Range Provided
in_rng_for_Formula.PasteSpecial xlPasteFormulas

End Function
